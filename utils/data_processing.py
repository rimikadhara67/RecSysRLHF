import pandas as pd

def load_dataset(file_path):
    return pd.read_csv(file_path)

def preprocess_data(data):
    # Perform tokenization, normalization, or other data preprocessing steps
    return data
